from interpreter import Interpreter

if __name__ == "__main__":
    # Process the database unless it is already
    print("work in progress")
    interpreter = Interpreter()
    interpreter.extract("requests/sample.txt")